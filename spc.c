#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main(int argc,char *argv[]){
if (argc<4){
    printf("");
    printf("");
}
char ip[16]={0};
strcpy(ip,argv[1]);
char first_port[6]={0};
strcpy(first_port,argv[2]);
char last_port[6]={0};
strcpy(last_port,argv[3]);
port_scanner

}